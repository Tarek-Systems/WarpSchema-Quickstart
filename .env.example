# =============================================================================
# WarpSchema Trial — Environment Configuration
# =============================================================================
# Copy this file:  cp .env.example .env
# Then fill in your trial license key (obtain from https://warpschema.io/trial).
# =============================================================================

# ---- License ----------------------------------------------------------------
WARPSCHEMA_LICENSE_KEY=trial_xxx

# ---- Gateway ----------------------------------------------------------------
GATEWAY_PORT=8000
DASHBOARD_PORT=8501
LOG_LEVEL=info            # debug | info | warning | error

# ---- PostgreSQL (Schema Registry) ------------------------------------------
POSTGRES_USER=warpschema
POSTGRES_PASSWORD=warpschema
POSTGRES_DB=warpschema

# ---- Redis (Embedding Cache) ------------------------------------------------
# Managed automatically by docker-compose; override only for external Redis.
# REDIS_URL=redis://redis:6379/0
